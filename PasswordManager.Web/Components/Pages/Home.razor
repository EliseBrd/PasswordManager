@page "/"
@using Microsoft.AspNetCore.Components.Authorization

@inject NavigationManager Navigation
@using PasswordManager.Web.Components.Vault

@* <div class="listeVaults">
    <VaultCard Title="Instagram"
               Username="john.doe@gmail.com"
               Password="SuperSecret123!"
               Category="Partagé" />

    <VaultCard Title="Banque"
               Username="henry.podico@gmail.com"
               Password="P@ssword2024"
               Category="Personnel" />
</div> *@
<VaultList Vaults="vaults" />

@code {
    private List<VaultList.VaultModel> vaults = new()
    {
        new VaultList.VaultModel
        {
            Id = 1,
            Name = "Famille",
            Category = "Personnel"
        },
        new VaultList.VaultModel
        {
            Id = 2,
            Name = "Coffre secret",
            Category = "Partagé"
        },
        new VaultList.VaultModel
        {
            Id = 2,
            Name = "Les copains",
            Category = "Partagé"
        },
        new VaultList.VaultModel
        {
            Id = 2,
            Name = "bebous",
            Category = "Partagé"
        },
        new VaultList.VaultModel
        {
            Id = 2,
            Name = "Crédit mutuel",
            Category = "Personnel"
        }
    };
}

</div>
<AuthorizeView>
    <Authorized>
        Hello, @context.User.Identity?.Name!
        @* <button class="nav-link btn btn-link" @onclick="@(() => Inspect(context))">Inspect</button> *@
        <form action="authentication/logout" method="post">
            <AntiforgeryToken />
            <input type="hidden" name="ReturnUrl" value="@(Navigation.Uri)" />
            <button type="submit" class="nav-link btn btn-link">
                <span class="bi bi-arrow-bar-left-nav-menu" aria-hidden="true"></span> Logout
            </button>
        </form>
    </Authorized>
    
    <NotAuthorized>
        <a href="authentication/login">Log in</a>
    </NotAuthorized>
</AuthorizeView>
   


